# Understanding the replacing of the string

# main_string.replace(old, new)


main_string = input('Enter main string: ')

new_string = input('Enter new string: ')

old_string = input('Enter old string: ')

# new = main_string.replace(old_string, new_string)

print(main_string.replace(old_string, new_string))
